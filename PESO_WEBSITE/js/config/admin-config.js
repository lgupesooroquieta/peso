/**
 * Admin configuration (client-side).
 * SECURITY NOTE: The admin code cannot be fully hidden in client-side code.
 * For production, consider using Firebase Remote Config or a backend endpoint.
 * Change ADMIN_SECRET_CODE to your desired secret before deploying.
 */
export const ADMIN_SECRET_CODE = "PESO_ADMIN_2024";
